[project]
name = "tweet-audit"
version = "0.1.0"
description = "Audit tweets on X against some sets of criterias and generates a list for deletion"
author = "Malomo Okikioluwa"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = [
    "."
]